public class lcs {

	// Find the longest common subsequence given two strings.
	public static void main(String args[]) {
		String a = "helloworld";
		String b = "lordeop";
		System.out.println(lcs(a, b));
	}

	private static String lcs(String a, String b) {
		/*
		int[][] mat = new int[a.length()][b.length()];
		int i = a.length() - 1;
		int j = b.length() - 1;

		for (; i >= 0; i--) {
			for (; j >= 0; j--) {
			}
		}
		*/
		return null;
	}
}
